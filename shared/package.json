{
  "name": "@chromapath/shared",
  "version": "1.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc --build",
    "clean": "tsc --build --clean",
    "dev": "tsc --build --watch"
  },
  "dependencies": {
    "@chromapath/client": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
