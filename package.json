{
  "name": "ChromaPath",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build:shared": "cd shared && pnpm install && pnpm run build",
    "build:api": "cd api && pnpm install && pnpm run build",
    "build": "pnpm run build:shared && pnpm run build:api",
    "start:api": "cd api && pnpm start"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
